# Machine-specific git configuration
# Copy this file to ~/.gitconfig (NOT symlink!)
# This allows 'git config --global' to work without affecting the dotfiles repo

# Include shared configuration from dotfiles
[include]
	path = ~/dotfiles/.gitconfig_global

[user]
	email = illumination.k.27@gmail.com
